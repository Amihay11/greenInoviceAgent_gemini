# Node modules
node_modules/
agent/node_modules/
GreenInvoice-MCP-main/node_modules/

# Secrets and environment variables
.env
agent/.env

# Local logs and session data
agent/agent.log
agent/agent-error.log
agent/whatsapp-auth/

# Phase 2: Shaul's long-term memory (SQLite) — local-only, never commit
agent/data/
*.db
*.db-shm
*.db-wal

# Portable binaries
node/
