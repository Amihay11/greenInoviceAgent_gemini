// MCP tool stub — fetches and parses a PDF/arXiv paper.
// Implement with `pdf-parse` or arxiv API in a follow-up.

export async function readPaper({ url }) {
  return {
    ok: false,
    stub: true,
    note: 'pdfArxivReader not implemented. Wire pdf-parse or arxiv SDK here.',
    url,
  };
}
